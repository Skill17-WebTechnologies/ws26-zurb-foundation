# Zurb Foundation 6.9.0 — WSC2026 minimal page

```bash
docker compose up --build
```

Open **http://localhost** — a responsive page (grid, callout, cards, button) styled with
Foundation 6.9.0 from the pinned npm package. Pinned: Node 24.1.0 / npm 11.5.0, Foundation 6.9.0.
